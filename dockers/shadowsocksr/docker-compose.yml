version: '2'
services:
  ssr:
    container_name: ssr
    build: ./ssr_file
    ports:
      - "15112:15112"
    restart: always
    logging: 
      driver: "json-file"
      options: 
        max-size: "100m"
